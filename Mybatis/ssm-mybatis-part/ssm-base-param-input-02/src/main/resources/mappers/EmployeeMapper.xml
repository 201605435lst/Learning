<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linebead.mapper.EmployeeMapper">

    <select id="queryEmpNameById" resultType="string">
      selete emp_name empName from t_emp where emp_id=#{id}
    </select>

    <select id="selectEmpNameAndMaxSalary" resultType="map">
        SELECT
        emp_name 员工姓名,
        emp_salary 员工工资,
        (SELECT AVG(emp_salary) FROM t_emp) 部门平均工资
        FROM t_emp WHERE emp_salary=(
        SELECT MAX(emp_salary) FROM t_emp
        )
    </select>

    <select id="queryAllEmp" resultType="employee">
        select * from t_emp
    </select>

    <select id="queryAllEmpName" resultType="string">
        select emp_name from t_emp where emp_salary > #{num}
    </select>



</mapper>