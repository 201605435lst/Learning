<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linebead.mapper.CustomerMapper">
    <resultMap id="custerMap" type="customer">
        <id column="customer_id" property="customerId" />
        <result column="customer_name" property="customerName" />
    <collection property="orders" ofType="order">
        <id column="order_id" property="orderId"/>
        <result column="order_name" property="orderName"/>
    </collection>

    </resultMap>

    <select id="queryList" resultMap="custerMap">
        SELECT  * from t_order o JOIN t_customer customer On o.customer_id=customer.customer_id
    </select>
</mapper>