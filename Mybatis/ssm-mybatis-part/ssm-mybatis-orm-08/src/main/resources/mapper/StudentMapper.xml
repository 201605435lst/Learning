<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linebead.mapper.StudentMapper">

    <resultMap id="BaseResultMap" type="com.linebead.pojo.Student">
            <id property="stuId" column="stu_id" jdbcType="INTEGER"/>
            <result property="stuName" column="stu_name" jdbcType="CHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        stu_id,stu_name
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from student
        where  stu_id = #{stuId,jdbcType=INTEGER} 
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from student
        where  stu_id = #{stuId,jdbcType=INTEGER} 
    </delete>
    <insert id="insert" keyColumn="stu_id" keyProperty="stuId" parameterType="com.linebead.pojo.Student" useGeneratedKeys="true">
        insert into student
        ( stu_id,stu_name)
        values (#{stuId,jdbcType=INTEGER},#{stuName,jdbcType=CHAR})
    </insert>
    <insert id="insertSelective" keyColumn="stu_id" keyProperty="stuId" parameterType="com.linebead.pojo.Student" useGeneratedKeys="true">
        insert into student
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="stuId != null">stu_id,</if>
                <if test="stuName != null">stu_name,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="stuId != null">#{stuId,jdbcType=INTEGER},</if>
                <if test="stuName != null">#{stuName,jdbcType=CHAR},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.linebead.pojo.Student">
        update student
        <set>
                <if test="stuName != null">
                    stu_name = #{stuName,jdbcType=CHAR},
                </if>
        </set>
        where   stu_id = #{stuId,jdbcType=INTEGER} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.linebead.pojo.Student">
        update student
        set 
            stu_name =  #{stuName,jdbcType=CHAR}
        where   stu_id = #{stuId,jdbcType=INTEGER} 
    </update>
</mapper>
